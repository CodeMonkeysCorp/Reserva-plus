spring:
  datasource:
    url: jdbc:h2:mem:reserva_plus_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: reserva-plus-test-secret-key-2026
    expiration-ms: 86400000
