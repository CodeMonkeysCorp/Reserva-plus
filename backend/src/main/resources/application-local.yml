spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://127.0.0.1:3310/reserva_plus?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USER:reserva_app}
    password: ${DB_PASSWORD:reserva123}

app:
  jwt:
    secret: ${JWT_SECRET:reserva-plus-local-dev-secret-key-2026}
